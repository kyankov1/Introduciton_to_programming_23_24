/*
#include <iostream>
using namespace std;

int main() {
	char symbol;

	cin >> symbol;

	if ((symbol >= 'a' && symbol <= 'z') ||
		(symbol >= 'A' && symbol <= 'Z')) {
		cout << "letter" << endl;

	}
	else if (symbol >= '0' && symbol <= '9')
		cout << "digit" << endl;
}

#include <iostream>
using namespace std;

int main() {
	int number;
	cin >> number;

	int last_digit = number % 10;
	int first_digit = number / 1000;

	//remove the last digit
	number /= 10;
	number %= 100; //number = number %100
	cout << last_digit << number << first_digit << endl;
	int result = last_digit * 1000 + number * 10 + first_digit;

	cout << result << endl;

		return 0;
}

#include <iostream>
using namespace std;

int main() {
	int day, month, year;
	cin >> day >> month >> year;

	if (day <= 0 || month <= 0 || year < 0) {
		cout << "Invalid date" << endl;
		return 0;
	}
	else if (month > 12) {
		cout << "Invalid date" << endl;
		return 0;
	}
	switch (month) {
	case 1, 3, 5, 7, 8, 10, 12:
		if (day > 31) {
			cout << "invalid date" << endl;
		}
		break;
	case 2:
		if (day > 23 && year % 4 != 0) {
			cout << "Invalid date" << endl;
		else if (day< 29 && !(year % 4))
		}
	case 4, 6, 9, 11:
		if (day > 30) {
			cout << "invalid date" << endl;
		}
		break;


	}
}

#include<iostream>

using namespace std;
int main() {
	int a, b;
	cin >> a >> b;

	while (b != 0) {
		int remainder = a % b;
		a = b;
		b = remainder;
	}
	cout << a << endl;

	return 0;
}

#include<iostream>
#include<cmath>
using namespace std;

int main() {
	int number;
	cin >> number;

	double sqrt_num = sqrt(number);
	for (int i = 1; i < -sqrt_num; i++) {
		if (number % i == 0) {
			cout << "Not prime" << endl;
			break;

		}
	}
	for (int i = 1; i <= sqrt_num && number % i == 0; i++) {

	}

	int j = 1;
	while (j < sqrt_num && number % j == 0) {
		j++;
	}
	
	cout << (j >= sqrt_num ? ("Prime") : ("Not prime")) << endl;

	return 0;
}
